# Copy this template to /etc/pdi/pdi.env on the runtime host.
# Never commit credentials or a populated environment file.
# Restrict the deployed file to its runtime owner (mode 600).
# Use server-local production endpoints; never use test databases or Mac tunnels.

DATABASE__URL=
NEXTCLOUD__URL=
NEXTCLOUD__USER=
NEXTCLOUD__PASSWORD=
IMMICH__URL=
IMMICH__API_KEY=
